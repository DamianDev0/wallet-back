export const BELVO_API_ENDPOINTS = {
  TOKEN: '/api/token/',
  LINKS: '/api/links/',
  ACCOUNTS: '/api/accounts/',
  TRANSACTIONS: '/api/transactions/',
  BALANCES: '/api/balances/',
} as const;

export const BELVO_WIDGET_ENDPOINTS = {
  ACCESS_TOKEN: '/api/token/',
} as const;
